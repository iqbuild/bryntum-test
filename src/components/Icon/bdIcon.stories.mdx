<!--- Icon.stories.mdx -->

import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import Icon from './bdIcon.vue';
import '../../styles/fontAwesome.scss';
import '../../styles/materialIcons.scss';

<Meta
	title="Components/Icon"
	component={Icon}
	parameters={{
		controls: {
			expanded: true,
			matchers: {
				color: /(background|color)$/i,
			},
		},
	}}
	argTypes={{
		name: {
			type: 'string',
			defaultValue: 'cogs',
			description: 'Corresponde al nombre del icono',
		},
		family: {
			options: ['FontAwesome', 'MaterialIcons'],
			control: { type: 'radio' },
			defaultValue: 'FontAwesome',
			description: 'Corresponde a la libería del icono',
		},
		type: {
			options: [
				'fas',
				'fab',
				'far',
				'fal',
				'fad',
				'material-icons',
				'material-icons-outlined',
			],
			control: { type: 'select' },
			defaultValue: 'fas',
			description:
				'Corresponde al estilo del icono. Para font awesome son los estilos "fa" y para material icons son los "material-icons"',
		},
		size: {
			control: { type: 'range', min: 1, max: 300, step: 1 },
			defaultValue: 22,
			description: 'Corresponde al tamaño del icono',
		},
		color: {
			control: { type: 'color' },
			defaultValue: '#000000',
			description: 'Corresponde al color del icono',
		},
	}}
/>

export const Template = (args) => ({
	components: { Icon },
	setup() {
		return args;
	},
	template:
		'<Icon :name="name" :family="family" :type="type" :size="size" :color="color"/>',
});

<Canvas>
	<Story name="Icon">{Template.bind({})}</Story>
</Canvas>
